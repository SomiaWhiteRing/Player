# 2026.9.2

- Android 导入页显示“从VIPRPG.org导入游戏”和游戏封面，导入前检查本地游戏及下载队列，已存在时只显示关闭按钮。
- 简化导入页：只保留窗口标题，突出封面和游戏名，使用“文件大小”，移除快照编号、来源域名、保存路径及说明段落，操作按钮改为同一行。
- 下载列表和通知仅在下载时显示速度；校验、保存统一显示“正在准备游戏…”，失败后显示“重试”。
- 点击导入后进入游戏列表，未完成游戏显示进度、下载速度和暂停／继续按钮，校验完成后才可启动。
- 支持通知栏后台下载、断点续传与任务恢复；暂停或失败的下载可移除。封面及续传需要网站同步更新导入接口。
- 修复 Android 主题属性、文件夹授权的 Lint 报错，并为已有新版返回回调的旧系统按键分支标注限定范围的 Lint 例外。
- 调整自动发布：每次成功构建更新 Nightly；仅在版本号提高时发布新的正式版，已有正式版保持不变。本次保持 2026.9.2，仅更新 Nightly。
- 同时提供 Windows x64、Web 和 Android 下载。
- 发布产物附带提交 SHA、构建链接、文件大小和 SHA-256，方便核对来源。

Android 包沿用现有 debug 签名。Web 部署须包含 ZIP 内的 HTML、JS、WASM 和 DATA 文件。
